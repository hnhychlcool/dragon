<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="titan.wms.common.dao.mapper.inside.InventoryFrozenDetailMapper">
    <!--auto generated Code-->
    <resultMap id="AllColumnMap" type="titan.wms.common.dao.meta.inside.InventoryFrozenDetailDO">
        <result column="id" property="id"/>
        <result column="inventory_id" property="inventoryId"/>
        <result column="frozen_type" property="frozenType"/>
        <result column="count" property="count"/>
        <result column="operator_id" property="operatorId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        `id`,
        <include refid="insert_column"/>
    </sql>

    <sql id="insert_column">
        `inventory_id`,
        `frozen_type`,
        `count`,
        `operator_id`,
        `create_time`,
        `update_time`
    </sql>

    <sql id="table_name">
        `inside_inventory_frozen_detail`
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="pojo.id">
        INSERT INTO <include refid="table_name"/>
        (
            <include refid="insert_column"/>
        ) VALUES (
            #{pojo.inventoryId},
            #{pojo.frozenType},
            #{pojo.count},
            #{pojo.operatorId},
            now(),
            now()
        )
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE <include refid="table_name"/>
        <set>
            <if test="pojo.id != null"> `id` = #{pojo.id}, </if>
            <if test="pojo.inventoryId != null"> `inventory_id` = #{pojo.inventoryId}, </if>
            <if test="pojo.frozenType != null"> `frozen_type` = #{pojo.frozenType}, </if>
            <if test="pojo.count != null"> `count` = #{pojo.count}, </if>
            <if test="pojo.operatorId != null"> `operator_id` = #{pojo.operatorId}, </if>
            <if test="pojo.createTime != null"> `create_time` = #{pojo.createTime}, </if>
            <if test="pojo.updateTime != null"> `update_time` = #{pojo.updateTime} </if>
        </set>
        WHERE id = #{pojo.id}
    </update>

    <select id="getByKeyForUpdate" resultMap="AllColumnMap">
        select <include refid="all_column"/>
        from <include refid="table_name"/>
        where `inventory_id` = #{inventoryId} and `frozen_type` = #{frozenType}
    </select>

    <delete id="delete">
        delete from <include refid="table_name"/>
        where id=#{id}
    </delete>

    <select id="findByInventoryId" resultMap="AllColumnMap">
        select <include refid="all_column"/>
        from <include refid="table_name"/>
        where `inventory_id` = #{inventoryId}
    </select>
</mapper>

